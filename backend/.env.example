# MongoDB Connection String
# Replace <username> and <password> with your MongoDB Atlas credentials
MONGO_CONNECTION_STRING=mongodb+srv://<username>:<password>@cluster0.nwnmmcl.mongodb.net/jobpath?retryWrites=true&w=majority

# JWT Secret Key (use the same one for all team members)
JWT_SECRET=your_jwt_secret_key

# Server Port
PORT=5000

# Frontend URL - for CORS configuration
# Default is Vite's default dev server port
FRONTEND_URL=http://localhost:5173
